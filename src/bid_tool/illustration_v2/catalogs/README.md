# Catalogs

Catalog files are generated or curated summaries consumed by the decision
layer. They should describe available templates and fallback renderers without
requiring callers to inspect renderer internals.

- `capability_catalog.json`: a human-readable mirror of the current static catalog.

